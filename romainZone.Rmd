---
title: "workstation"
author: "RF"
---


TODO

check package structure
add ggplot2 dependency and dplyr
plotevotrait outputs an empty list with the plot

plotDynamics has an error more than 9 species have to been plotted (not enough colors) need to do a case about that

```{r}
library(ggplot2)
library(dplyr)
```




```{r}
params <- evoParams(no_sp =  5)

sim <- project(params)

plotBiomass(sim)

plotDiet(sim@params, species = "11")

simEvo <- evoProject(params = params, t_max = 200, mutation = 3, trait = "beta")

plotDynamics(simEvo)

plotevoTrait(simEvo, traitID = "beta", returnData = F)

```

